name: 'call_workflow_Upload Python files to ec2'

on:
  push:
    branches:
      - dev
    paths:
      - 'App/functions/**'
  workflow_dispatch:
  
jobs:
  call-workflow:
    uses: infra-faci-pf/repo_infradx_cicd_main/.github/workflows/called_py_workflow.yml@main
    secrets: inherit
    with:
      AWS_ACCOUNT: 814396194035                                    # AWSアカウントID
      IAM_ROLE_NAME: infradx-dev-iam-role-oidc                     # IAM Role名
      INSTANCE_ID: "i-040dcc2eaefbce22a"                           # インスタンスID
      BUCKET_NAME: "infradx-dev-s3-bckt-cicd-814396194035"         # S3バケット名
      REPO_PATH: "App/functions/"                                  # リポジトリパス
      REPO: "repo_infradx_ap_PCM_0010"                             # リポジトリ名
      INSTANCE_PATH: "/infradx/app/infradx-batch-data/functions"   # インスタンスパス
